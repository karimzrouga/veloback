package com.itgate.ecommerce.repository;

import com.itgate.ecommerce.models.EquipeFederation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EquipeFederaRepository extends JpaRepository<EquipeFederation,Long> {
	
}
